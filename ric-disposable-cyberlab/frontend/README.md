# Frontend

For the MVP, prefer using CTFd rather than building a custom CTF website from scratch.

Only add custom frontend work if it directly supports lab launch/status and the core prototype is already working.
